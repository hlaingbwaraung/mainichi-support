{
  "name": "mainichi-support-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "googleapis": "^148.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "overrides": {
    "uuid": "11.1.1"
  }
}
